import 'package:flutter/material.dart';

class KuisMateriTiga extends StatelessWidget {
  const KuisMateriTiga({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}