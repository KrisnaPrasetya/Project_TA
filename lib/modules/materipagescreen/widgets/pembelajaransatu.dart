import 'package:flutter/material.dart';

class Pembelajaransatu extends StatelessWidget {
  const Pembelajaransatu({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Pengertian Kubus dan Balok'),
      ),
    );
  }
}
